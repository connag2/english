# Codex용 최종 프롬프트 (한국어, 데스크톱 완성도 우선)

아래 요구사항으로 Python 기반 로컬 영단어 암기 프로그램을 구현해줘.

## 프로젝트 방향
이 프로그램은 단순 단어장이 아니라,
- 단어를 쉽게 넣고
- 보기 편하게 관리하고
- 자동으로 복습하고
- 오답을 반복하며
- 계속 켜고 싶어지는

**가볍고 안정적인 개인용 영단어 암기 프로그램**이어야 한다.

핵심 우선순위:
1. 입력 편의성
2. UI 가독성
3. 안정성(저장/정렬/중복/버튼 연타)
4. 반복 학습 흐름

> 지금 단계는 **작은 기기 대응보다 데스크톱 완성도**를 우선한다.

---

## 1) 기술/구조 기준
- Python 3.13
- PySide6
- JSON 저장
- 모듈화 구조

기본 파일 구조:
```
main.py
models.py
storage.py
word_manager.py
study_engine.py
review_scheduler.py
stats_manager.py
ui/main_window.py
ui/add_words_view.py
ui/wordbook_view.py
ui/study_view.py
ui/result_view.py
```

---

## 2) 단어 입력: 내부 규칙 1개 + 입력 방식 3개

### 내부 기본 문법
`word=meaning1|meaning2|meaning3`

예:
```
apple=사과
run=달리다|운영하다
light=빛|가벼운|밝은
```

### 입력 방식 (필수)
1. 직접 입력
   - 영어 입력칸
   - 뜻 입력칸
   - 추가 버튼
2. 여러 줄 일괄 입력
   - 큰 텍스트 박스
   - 미리보기 버튼
   - 중복 정리 결과 표시
   - 저장 버튼
3. txt 파일 불러오기
   - 동일 문법 자동 파싱
   - **즉시 저장 금지, 미리보기 후 저장**

---

## 3) 입력 파싱 규칙 (강제)
- 줄 단위 처리
- 빈 줄 무시
- 앞뒤 공백 제거
- 영어 단어 소문자 통일
- `=` 없는 줄은 오류 목록으로 분리
- 뜻은 `|` 기준 분리
- 뜻 앞뒤 공백 제거
- 빈 뜻 제거
- 같은 단어는 뜻 자동 병합
- 같은 뜻 중복 제거

예:
```
 apple = 사과
APPLE=과실
apple=사과
```
결과:
```json
{
  "word": "apple",
  "meanings": ["사과", "과실"]
}
```

---

## 4) 저장 구조 (JSON)
초기 버전은 SQLite 대신 JSON 기반으로 구현.

```json
{
  "words": [
    {
      "word": "apple",
      "meanings": ["사과"],
      "level": 0,
      "correct": 0,
      "wrong": 0,
      "last_review": null,
      "next_review": null,
      "favorite": false
    }
  ],
  "meta": {
    "version": 1
  }
}
```

---

## 5) 화면 구성 요구사항

### 메인 화면(대시보드)
한눈에 표시:
- 전체 단어 수
- 오늘 복습 필요 단어 수
- 오늘 학습한 단어 수
- 최근 틀린 단어

큰 버튼:
- 학습 시작
- 복습 시작
- 단어 추가
- 단어장 보기

### 단어 추가 화면
입력 방식 전환:
- 직접 추가
- 여러 줄 추가
- 파일 불러오기

특히 여러 줄 입력 화면 필수 요소:
- 입력창
- 미리보기 표
- 오류 줄 목록
- 저장 버튼

### 단어장 보기 화면
필수 기능:
- 목록
- 검색
- 수정/삭제
- 뜻 추가/삭제
- 정렬(알파벳순, 최근 추가순, wrong 많은 순)

### 학습 화면
필수 요소:
- 현재 문제 번호
- 단어 또는 뜻 크게 표시
- 입력칸 또는 객관식 선택지
- 정답 확인
- 모르겠어요
- 다음 문제

### 결과 화면
- 맞은 개수
- 틀린 개수
- 정답률
- 오답 다시 보기
- 오답만 다시 풀기
- 종료

---

## 6) 학습/복습 로직

### 학습 모드 (최소 2개)
1. 영어 → 뜻
2. 객관식 4지선다

### 학습 동작
- 랜덤 출제
- 복습 필요 단어 우선 출제 옵션
- 틀린 단어 자동 오답 목록 추가
- 세션 종료 후 오답만 재시험 가능

### 단어별 상태 필드
- level
- correct
- wrong
- last_review
- next_review

### 간단 SRS 규칙
- 정답: correct 증가, level 증가
- 오답: wrong 증가, level 일부 감소 가능
- 오답 단어는 오늘 다시 우선 출제
- 정답 단어는 next_review 자동 계산

복습 간격 예:
- level 0 → 1일
- level 1 → 3일
- level 2 → 7일
- level 3 → 14일
- level 4+ → 30일

---

## 7) 안정성 요구사항 (중요)
- JSON 파일 없으면 자동 생성
- JSON 파일 손상 시 백업 후 기본 구조 복구
- 저장 버튼 연타 시 중복 저장 방지
- 입력 오류가 있어도 앱 비정상 종료 금지
- 수정/삭제/정렬 직후 UI 즉시 갱신
- import 누락/연결 누락 없이 즉시 실행 가능

---

## 8) UI 스타일 가이드
- 복잡한 개발자용 화면처럼 만들지 말 것
- 버튼 너무 작지 않게
- 구역 분리 명확히
- 여백/가독성 우선
- 한 화면에 기능 과밀 배치 금지
- 초보자도 바로 흐름 이해 가능해야 함

---

## 9) 개발 순서 (권장)
1. 입력 시스템 완성 (직접/여러 줄/파일/미리보기/오류/중복)
2. 단어장 관리 완성 (목록/검색/수정/삭제/정렬)
3. 학습 모드 완성 (영→뜻/객관식/오답 재시험/결과)
4. 복습 자동화 (level/next_review/우선 출제)
5. UI 정리 (버튼/여백/상태 표시)
6. 이후 작은 기기 대응

---

## 10) 출력 형식 요구
- 여러 파일을 **완전한 코드**로 출력
- 파일별 코드 블록 분리
- 실행 진입점은 `main.py`
- 실행 방법 포함 (`pip install ...`, `python main.py`)
- 함수 역할 분리 및 주석 제공

---

## 11) 핵심 지시 요약
우선 작은 기기 최적화는 나중에 하고,
지금은 데스크톱 기준으로 완성도와 안정성을 높여라.

특히 다음을 우선해라:
1. 단어 입력 경험 개선
2. 단어장 관리 기능 강화
3. 학습 흐름 단순화
4. 자동 복습 시스템 추가
5. UI 가독성 개선
6. 저장/정렬/중복 처리 안정성 확보
